# snic

snic (Scala Native Interactive CLI) is a library for making interactive CLIs with
Scala Native, since JLine only works for the JVM. Trying to make it as much like JLine
as possible but it won't be anywhere near as good.
